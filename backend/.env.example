# Django Backend Environment Variables

# SECURITY WARNING: Generate a new SECRET_KEY for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=django-insecure-s=qfm^ph-@znr*n6tw+a=eu-(=d(+0@f*b4$tggdt8ack$nguz

# Set to False in production
DEBUG=True

# Comma-separated list of allowed hosts
# Local: * or localhost,127.0.0.1
# Production: your-domain.onrender.com
ALLOWED_HOSTS=*

# Frontend origins for CORS/CSRF (comma-separated)
# Add your production frontend URL here
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://bilimdeck.vercel.app
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://bilimdeck.vercel.app

# Database (optional - defaults to SQLite)
# For Render Postgres, use DATABASE_URL from Render
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Django Admin Superuser (for automated creation)
# Use these on Render to auto-create admin user on first deploy
# Run: python manage.py createsuperuser --noinput
# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_EMAIL=admin@example.com
# DJANGO_SUPERUSER_PASSWORD=your-secure-password-here
